let surname = "Yuri";
const num = 20345698;
let ddkdkdk = true;
type TObject = {
    model: string,
    year: number,
    have: boolean
}
let jnjn: string[]
function country(a: string, b: number) {
    return a + b;
}